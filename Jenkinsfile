pipeline {
	agent any
	stages {
		stage('Build') {
			steps {
				echo "Building"
			}		
		}
		stage('Test') {
			steps {
				echo "Building"
			}		
		}
		stage('Deploy') {
			steps {
				echo "Building"
			}		
		}
	}
	post {
		success {
			echo "Ok"
		}
	}
}
